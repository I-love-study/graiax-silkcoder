name: build multi version of python

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version:
        - "3.8"
        - "3.9"
        - "3.10"
        architecture:
        - x64
        - x86
      
    name: build ${{ matrix.python-version }} ${{ matrix.architecture }} sample
    steps:
      - uses: actions/checkout@v2

      # 很可惜，因为 Setup PDM 并不会给你 C extension 环境支持，所以还要 setup python
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
          architecture: ${{ matrix.architecture }}
      
      - name: Get Lastest Environment
        run: |
          python -m pip install --upgrade pip
          pip install setuptools wheel twine pdm

      - name: Build and Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          python -m pdm build -v
          twine upload --skip-existing dist/*